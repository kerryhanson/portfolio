# Set to true to show the Config menu (theme picker + Workshop) in the global nav.
# VITE_SHOW_CONFIG_MENU=true

# Home page hero visual: image | none | flow-diagram | placeholder
# VITE_HERO_VISUAL_MODE=flow-diagram

# Site access gate (client-side — soft barrier only). Enabled by default.
# VITE_SITE_ACCESS=false
# VITE_SITE_PASSWORD=Summer2026
